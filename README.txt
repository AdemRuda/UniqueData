1. Set up a local server (e.g., XAMPP or WAMP).
2. Import the `create_table.sql` file into phpMyAdmin to create the database and table.
3. Place the `UniqueEmployeesData` folder in the `htdocs` (XAMPP) or `www` (WAMP) directory.
4. Access the form via `http://localhost/UniqueEmployeesData/index.html`.
5. Set up a cron job to run `visa_notification.php` daily for visa expiration notifications.
6. All Rights Reserved Adem Ruda 2025
7. Web Developer Adem Ruda